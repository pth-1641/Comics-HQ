const crawlBaseUrl = import.meta.env.VITE_CRAWL_BASE_URL;
const proxyServer = import.meta.env.VITE_PROXY_SERVER;
const imageProxyServer = import.meta.env.VITE_IMAGE_PROXY_SERVER;

export { crawlBaseUrl, proxyServer, imageProxyServer };
